# Методы вычисления математических функций

Имплементировать `float` функции натурального логарифма `ln` с точностью `< 3.5 ulp` в режиме округления `TO_NEAREST` на `C`. Диапазон аргумента $2^{-126} \leqslant x < 2^{127}$. Решение должно иметь вид:

```cpp
float logf(float x) {
  ...

  return res;
}
```

Решение должно быть корректным на всем указанном диапазоне аргументов. Использовать функции сторонних библиотек, например gnu libc, нельзя.
